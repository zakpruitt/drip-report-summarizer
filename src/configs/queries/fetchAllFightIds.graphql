query FetchAllFightIds($reportCode: String!, $killType: KillType!, $difficulty: Int!) {
    reportData {
        report(code: $reportCode) {
            fights(difficulty: $difficulty, killType: $killType) {
                id
            }
        }
    }
}
